[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pricelib"
version = "1.5.0"
description = "Example fixture package for the apidrift docs."

[tool.setuptools.packages.find]
where = ["src"]
